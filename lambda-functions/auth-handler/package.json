{
  "name": "auth-handler",
  "version": "1.0.0",
  "description": "JWT validation and authorization service",
  "main": "index.js",
  "scripts": {
    "test": "node index.js",
    "deploy": "zip -r auth-handler.zip . -x 'node_modules/*' '*.zip' && aws lambda update-function-code --function-name college-hq-auth-handler --zip-file fileb://auth-handler.zip"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "jwk-to-pem": "^2.0.5",
    "axios": "^1.6.5"
  }
}