{
  "name": "profile-service",
  "version": "1.0.0",
  "description": "User profile management service",
  "main": "index.js",
  "scripts": {
    "test": "node index.js",
    "deploy": "zip -r profile-service.zip . -x 'node_modules/*' '*.zip' && aws lambda update-function-code --function-name college-hq-profile-service --zip-file fileb://profile-service.zip"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0"
  }
}