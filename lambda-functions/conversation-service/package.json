{
  "name": "conversation-service",
  "version": "1.0.0",
  "description": "Conversation history management service",
  "main": "index.js",
  "scripts": {
    "test": "node index.js",
    "deploy": "zip -r conversation-service.zip . -x 'node_modules/*' '*.zip' && aws lambda update-function-code --function-name college-hq-conversation-service --zip-file fileb://conversation-service.zip"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/lib-dynamodb": "^3.500.0",
    "dotenv": "^16.4.5"
  }
}